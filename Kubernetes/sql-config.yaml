apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: nate-agent
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS users (
      id SERIAL PRIMARY KEY,
      username TEXT NOT NULL,
      email TEXT NOT NULL UNIQUE
    );
    INSERT INTO users (username, email) VALUES ('nate', 'nate@example.com');